# AriaAI 技术建议

> 更新日期：2026-04-12
> 说明：基于最新主线代码

---

## 1. 总体判断

当前技术工作的主轴已经很清楚：

- 统一配置
- 拆分聊天编排
- 收敛设置体系
- 清理工程边界
- 为 RAG 和导出等高价值能力补完整产品链路

这轮代码已经从“方向正确”走到“开始形成结构”。

---

## 2. 当前最值得继续推进的方向

### 2.1 统一 provider 与模型选择

现状：

- `provider_selector.py` 已落地
- `settings_helper.py` 已提供类型化访问

这是对的，下一步应继续避免在业务层直接自己解析 provider/model。

### 2.2 继续拆解聊天编排

现状：

- `context_builder.py`、`provider_selector.py`、`title_generator.py` 已出现

下一步：

- 继续把 `chat.py` 拆成更清晰的 orchestration、export、message persistence 子职责

### 2.3 明确设置体系

现状：

- `SETTINGS_HIERARCHY.md` 已经把配置分成三层

下一步：

- 把这套分层真正落实到前后端交互与运维流程里

### 2.4 继续推进仓库边界治理

现状：

- `backend/.gitignore` 已加入
- 运行产物已从主线移除

下一步：

- 确认本地运行不会再把 data/uploads 带回版本库

---

## 3. 当前仍需解决的技术问题

### 3.1 残余乱码

这是当前最影响体验和长期维护的问题。

### 3.2 枚举未完全收敛

虽然已有统一枚举入口，但仍有标签层问题。

### 3.3 RAG 仍偏 baseline

虽然已有结构化引用结果，但距离更强的检索与解释性仍有距离。

---

## 4. 建议实施节奏

### Sprint D

- 修乱码
- 收敛枚举标签
- 校准默认配置策略

### Sprint E

- 继续拆 `chat.py`
- 把导出能力抽象成更稳定的后端/前端接口层
- 强化设置体系的 UI 表达

### Sprint F

- RAG 升级
- 生成物管理增强
- 更完整的观测性

---

## 5. 成功标准

如果下面这些成立，说明技术收敛进入更稳阶段：

- 前后端 provider / model 选择逻辑一致
- `chat.py` 复杂度明显下降
- 设置体系对开发、运维、用户都清晰
- 用户可见文案基本无乱码
- Web 与后端枚举完全一致
