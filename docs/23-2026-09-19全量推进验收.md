# 2026-09-19 全量推进与验收

基线：`4259630`。用户授权推进项目盘点中的全部待办。续验至 2026-09-20。产品介绍母本为用户未提交文件，保留原样。本轮主要变更及运维读取修复已以 6 个提交推送到 main，生产运行版本为 `1bb53c1046d146cae6e1c3ede30778941b482eb9`。[最终部署](https://github.com/guoliang1114-boop/AriaAI/actions/runs/35455084068)、语义预检、Provider 评测和生产 PostgreSQL E2E 均通过。后续验收文档提交不改变该运行版本。

## 工作清单

| 工作项 | 验收要求 | 状态 |
|---|---|---|
| 知识语义检索 | 本地语义模型、明确向量身份、权限前置、混合检索、可恢复重建与中文样例评测 | 本地及生产合成评测通过，现有资料探索性抽查完成；广泛业务验收与生产启用仍待执行 |
| 对话质量 | 同资料范围的受限指代追问、Markdown 精确重复保护及回归 | 已实现，焦点及受影响回归通过 |
| 延迟治理 | 多 Provider 无正文观测，按模型统计 P50/P95，失败/取消不伪装成功 | 已上线；waiting_confirmation、普通成员拒绝与无正文返回通过；样本不足以判断稳定分位数 |
| Skill 交付 | 合成交付示例与验证材料，保留专业判断和事实边界 | 48 包静态验收通过；逐包真实产物及业务判断验收仍待完成 |
| 前端技术债 | 53 条 lint 警告逐项修复，测试与构建通过 | 0 errors / 0 warnings，全量测试及构建通过 |
| 运维收口 | 复核旧知识库读取退役条件、陈旧记忆与失败回执，经 Aria 原生权限处理 | 读取副作用已修复上线，线上状态对比通过；业务失败处理与兼容入口退役未执行 |
| 文档与任务 | 更新过期权限/版本说明、贡献者指南、发布说明、Issue/PR 验收状态 | 实现和文档已合并；按具体验收关闭已完成事项，保留路线图与安全跟踪项 |
| 综合验收 | 当前实现的焦点测试、受影响回归、数据库 E2E、真实模型评测与线上检查 | 新版本部署、生产数据库/模型验证及线上检查全部通过，证据如下 |

## 基线证据

- 2026-09-13 部署成功：前端 663 测试、后端所选回归 1,254 测试；前端 lint 53 warnings。
- 确定性对话质量门：141 场景、37 指标。
- Skill 静态质量：48 包，平均 82.23，95 warnings，7 个 C 级；静态分不代表业务交付质量。
- Alembic：`054_v1_54` 唯一 head。
- GitHub：8 个未分配开放 Issue；PR #62 的核心分包方案已存在于主干。

## 实施与验证记录

### 当前实现

- 前端 `npm run lint`：0 错误、0 警告；`npm test` 全量通过（97 个当前测试文件），`npm run build` 通过。真实浏览器已核对本地构建的登录页并保存空账号截图至 `docs/assets/aria-login-preview.png`。
- 后端首批知识检索、资料连续性、Provider 观测、延迟报表、Markdown、Harness 与事件合约焦点检查：220 passed。
- 受影响的发布回归首轮：1,294 passed。新增管理员拒绝与评测恢复检查 5 passed；Markdown 完整执行循环和工具 transcript 回归 18 passed。纳入所有新增用例及部署合约后的最终发布回归：**1,301 passed**，耗时 92.34 秒；子集与总数重叠，不应相加。
- 独立本机 PostgreSQL 16.13：125 passed，覆盖新版知识权限、任务恢复、存储原子性、会话连续性与项目问题 resolution；没有连接生产库。未启用 native pgvector，因此不覆盖该部署矩阵。
- 确定性项目对话质量门：141 场景、37 指标全部通过。
- 检索评测：真实检索路径的离线子集 5/5、本地 FastEmbed 语义模式 12/12。预热查询 P50 6 ms / P95 8 ms；embedding 准备 185.10 ms。小型合成语料，不代表真实业务召回或全站提速。
- Skill 静态质量：48 包全部 A，平均 100，0 warnings / 0 errors；只说明结构、样例与验收材料齐全，不等于已通过业务交付验收。
- Alembic：`054_v1_54` 唯一 head；本轮无数据库模型变更或新增迁移。
- 部署与数据库 E2E workflow 加入新增回归；合成测试显式固定 hash Provider，不受生产语义模型缓存或下载影响。真实语义模型评测独立运行。
- 两个 workflow 的 YAML 和内嵌 Python 均通过解析检查；8 份入口/验收文档的本地链接核对无缺失，`git diff --check` 通过。

### 发布过程中发现的运维读取副作用

- 2026-09-20 00:20（北京时间），原生管理员查询 `/memory/operations/summary` 时，旧实现间接调用客户任务恢复逻辑，按 stale/零版本/queued/rebuilding 条件安排重建，并可能清除失败时间。这不是纯读取，也不构成业务重建授权。
- 后续只读单对象状态显示，客户 ID 3、4 分别在 00:21:25、00:21:54 重建失败；内容版本仍为 3、6，内容更新时间未变化。没有进一步调用重建、取消或清除回执接口。
- 修复删除 GET 的隐式恢复路径；`/clients/memory/jobs` 和汇总接口仅展示调度器及 `status_only` 状态。恢复继续使用原生写入口及权限，AI 业务写入继续要求 HITAS。
- 新增/调整的任务测试 11 passed；客户/项目记忆权限、原生运维状态、槽位和 API 受影响回归 152 passed、1 skipped（既有已移除售前模板 bootstrap 用例）。修复已被最终部署和 PostgreSQL E2E 测试覆盖。

### 新版本发布与业务资料抽查

- [最终部署 `1bb53c1`](https://github.com/guoliang1114-boop/AriaAI/actions/runs/35455084068)：前端 663 测试及零警告 lint 通过；服务器发布回归 **1,300 passed**，802.13 秒。两个既有环境/事务 warning 保留记录；141 场景质量门、hash 5/5、经验证的备份、迁移治理、重启与健康检查通过。
- [生产语义预检](https://github.com/guoliang1114-boop/AriaAI/actions/runs/35456008356)：FastEmbed `BAAI/bge-small-zh-v1.5`，**12/12**；模型准备 28,877.64 ms，预热查询 P50 26 ms / P95 69 ms。评测、embedding、检索三个部署文件 SHA-256 与本地发布代码一致。仅合成语料；没有变更生产配置或业务索引。
- [新版本 Provider 评测](https://github.com/guoliang1114-boop/AriaAI/actions/runs/35458770316)：Kimi `kimi-k3`，**6 场景通过**，首答通过率 0.8333、1 个经过质量修正；最终事实/引用/弃答门槛全部通过，总耗时 72,273 ms。
- [新版本生产 PostgreSQL E2E](https://github.com/guoliang1114-boop/AriaAI/actions/runs/35458772986)：**1,215 passed**，1,149.23 秒。PostgreSQL 18 原生备份校验通过；前后 public 表数 78、Alembic `054_v1_54` 不变，隔离测试 schema 已删除。
- 最终线上检查：健康接口、入口与 5 个引用资源均 HTTP 200；匿名知识检索 HTTP 401；现有文档 2 个词法问题及分块来源核验通过，无关问题与空项目范围返回空结果。管理员延迟接口无正文，最终 7 天窗口无有效 Run；8 天和 90 天窗口均能读到先前的 2 次 Run、每模型 1 次，属于滚动窗口差异，样本不足以代表稳定 P50/P95。
- 最终运维页读取前后客户 3、4 的版本、更新时间、失败时间与 failed 状态完全一致，调度任务列表为空。失败仍待按原生授权/HITAS 处理，没有清除或伪装成功。
- 首轮 `9a79062` 部署：前端 97 文件 / 663 测试通过、lint 无警告；服务器 SQLite 发布回归 1,298 passed，耗时 799.38 秒；141 场景质量门和 hash 检索 5/5 通过。备份、迁移治理、重启与健康检查通过。最终只读修复版本另验，不能混用两轮计数。
- 线上入口 3 个 modulepreload，入口及预加载脚本均 HTTP 200；与主干的保守分包实现一致。未据此声称慢网 DOMContentLoaded 已缩短多少。
- 使用原生管理员读权限抽查现有文档 1 的 14 个分块，在本机内存数据库中分别执行 hash 检索和原生语义分块/embedding/检索。语义模式得到 133 个临时短分块；宏观环境、五力、备选战略三个问题均在前 3 条找到相关资料。该流程从既有分块重切，存在原分块重叠，不等于完整原件的生产重建。
- 这三个问题属于探索性抽查，未构成冻结的独立业务评测集。宏观环境的初始预期编号遗漏了另一段 PEST 证据，已阅读实际内容后修正；直接为旧大分块生成向量的早期比较也不代表原生重建路径。生产仍保持 hash，未修改配置或重建业务索引。
- README、CONTRIBUTING、文档索引、登录截图的远端 Git blob 与本地一致。Issue #71、#72、#73、#76、#77 已关闭；重复 PR #62 已关闭且未合并旧分支。

环境说明：首次后端调用因本机默认 PostgreSQL 未启动未能建立 fixture；临时内存 SQLite 又因 HTTP 工作线程连接隔离不适合该组 API 测试。改用独立 SQLite 文件后通过，并另启独立 PostgreSQL 实例完成数据库回归。未修改应用数据库或放宽断言。当前 Python 3.9 / LibreSSL 环境仍有 urllib3 兼容警告；多轮 golden 测试另有既有 SQLAlchemy transaction warning，测试通过，不能据此称运行环境零警告。

### 已部署基线（不代表当前工作区已发布）

- [2026-09-19 Production Database E2E](https://github.com/guoliang1114-boop/AriaAI/actions/runs/35451244983)：基线 `4259630`，1,169 passed。
- [2026-09-19 Provider Grounded QA Eval](https://github.com/guoliang1114-boop/AriaAI/actions/runs/35451246996)：Kimi `kimi-k3`，6 个合成资料真实模型场景通过；首答场景通过率 0.8333，其中 1 个经过质量修正。最终事实/引用/弃答等门槛通过，总耗时 67,097 ms。不是六个场景全部首答成功。
- 基线只读知识报告：1 份 legacy 已映射到新版，14 个分块，cutover ready=true；兼容 fallback 与显式 legacy 选择仍开启。
- 基线只读记忆报告：33 个项目有 24 个 stale 槽位，18 个客户有 16 个 stale 槽位；项目/客户各有 2 个原生 last_failure 状态，原生状态无缺失/分歧。未重建业务记忆、删除回执或退役旧读取入口。具体复核路径见 [发布说明](25-Agentic-Workspace-Preview-发布说明.md)。
- 本轮只读 `/api/health` 返回 `status=ok`、`service=AriaAI`、`version=0.0.4`。健康响应不能证明新代码已部署。

## Issue / PR 验收映射

下表记录当前验收与远端状态。未发送 Issue/PR 评论；关闭重复 PR 不等于合并旧分支。

2026-09-22 续验：安全边界版本 `52488f9` 已发布；服务器 1,473 项回归和 Kimi `kimi-k3` 9 场景 / 11 回合注入评测通过，见 [28 验收](28-摘要来源边界与注入对抗验收.md)。生产业务记忆及知识索引未自动重建。

同日知识库续验：`33df8e3` 已发布，服务器 1,512 项回归通过，真实 139 页 PPT 副本在词法/语义模式下各 9/9 项通过，见 [29 验收](29-知识入库与真实资料验收.md)。线上原件、既有索引和查询复核通过；语义配置启用及原生业务重建仍与代码验收分开。

| 跟踪项 | 当前证据 | 剩余条件 |
| --- | --- | --- |
| [#69 知识库](https://github.com/guoliang1114-boop/AriaAI/issues/69) | 已关闭；Office/PDF 入库修复随 `33df8e3` 发布，原生权限/索引/引用回归与真实文件副本的词法/语义各 9/9 项验收通过，见 [29 验收](29-知识入库与真实资料验收.md) | 本 Issue 的实现及稳定性验收完成；生产语义启用、业务重建和后续 OCR 等扩展为独立工作 |
| [#70 Run Harness](https://github.com/guoliang1114-boop/AriaAI/issues/70) | 已关闭；17 类事件的前后端类型/运行时兼容性、终态处理、回执保留及 CI 门禁随 `6559d84` 发布，见 [27 验收](27-Run-Harness合同兼容性验收.md) | 无本 Issue 剩余项；新增协议能力继续走合同变更检查 |
| [#71 英文上手](https://github.com/guoliang1114-boop/AriaAI/issues/71) | 已关闭；README、贡献者指南与截图已合并，远端 blob 核验一致 | 无本 Issue 剩余项 |
| [#72 Skill 样例](https://github.com/guoliang1114-boop/AriaAI/issues/72) | 已关闭；编写/验证示例、输入与产物要求、合成案例和检查材料已补齐 | 逐包真实业务交付验收仍是独立任务，不由静态分替代 |
| [#73 发布准备](https://github.com/guoliang1114-boop/AriaAI/issues/73) | 已关闭；发布说明、维护者检查、限制、回滚流程与本轮 commit / Actions 证据已完成 | 无本 Issue 剩余项；生产业务操作限制继续保留 |
| [#74 安全边界](https://github.com/guoliang1114-boop/AriaAI/issues/74) | 已关闭；摘要来源隔离、ACL/记忆/HITAS 回归与真实模型 9 场景 / 11 回合评测通过，随 `52488f9` 发布，见 [28 验收](28-摘要来源边界与注入对抗验收.md) | 无本 Issue 剩余项；固定语料不代表穷尽所有注入风险，持续安全回归仍为开发要求 |
| [#76 文档索引](https://github.com/guoliang1114-boop/AriaAI/issues/76) | 已关闭；用途、状态、阅读路径和 Issue 映射已合并，远端 blob 核验一致 | 无本 Issue 剩余项 |
| [#77 测试覆盖](https://github.com/guoliang1114-boop/AriaAI/issues/77) | 已关闭；焦点、发布回归、独立 PostgreSQL、质量门以及新版本生产模型/数据库验收均通过 | 无本 Issue 剩余项；持续测试仍为开发要求 |
| [PR #62](https://github.com/guoliang1114-boop/AriaAI/pull/62) | 已关闭，未合并；核心实现已在主干，线上 3 个 modulepreload 已验证 | 慢网端到端性能需独立测量 |

本轮没有把剩余生产操作标记完成；查询引发的旧恢复副作用已单独记录，没有为通过验收而清除失败回执、改写旧消息或修改产品介绍母本。
