# AriaAI 技术债与行动清单

> 更新日期：2026-05-15
> 合并自：代码问题清单（02）、全面技术评估（09）、技术评估与行动建议（11）
> 版本基准：V0.0.2，commit 407ee5c
>
> 技术栈：React 19 + TypeScript 5.9 + Vite 8 + Tailwind CSS 4 | FastAPI 0.115 + SQLModel + Alembic (PostgreSQL) | 多 LLM (Claude/Kimi/Deepseek/BigModel/MiMo) + RAG + SSE | GitHub Actions CI/CD → SCP/SSH → PM2 + Nginx

---

## 1. 总体判断

### 产品方向对，功能已超过验证深度

**已经做对的**：

- 战略定位准确：「客户关系智能系统」比「AI 协作工作台」更有护城河，北极星场景（会前 30 秒）定义尖锐。
- 核心资产模型已成型：项目记忆 → 客户记忆 → 结构化干系人 → 跨项目沉淀，分层架构扎实。
- 关键闭环已跑通：Skill 从项目/客户空间启动 → Chat 执行 → 保存为文档 → 触发记忆刷新。
- 会前简报 V1 已落地，不调用 LLM（纯聚合），产品表达克制。

**但最大的判断是：功能已经过度建设了。**

当前已完成：10 个 Alembic 迁移版本、记忆快照/diff/回滚、任务治理服务端统计、多视角摘要缓存、结构化干系人全链路、项目空间重构第一阶段。这些都很对，但风险在于：**团队在用工程技术假设用户需求，而不是让用户行为告诉产品该建什么。**

### 架构在进化，安全和运维是短板

| 维度 | 现状 | 风险 |
|------|------|------|
| 安全 | 3 个 P0 漏洞已修复，仍有剩余项 | 数据泄露一次，前面所有建设归零 |
| 备份 | 已建立定时备份策略 | 需验证恢复流程 |
| 运维 | Nginx 已配 HTTPS + 安全头 + gzip | 监控、日志聚合仍缺失 |
| 前端稳定性 | ErrorBoundary 已落地 | 需扩展到更多页面 |
| 测试 | 后端 55%，前端真实约 30% | 核心模块仍有 0% 覆盖 |
| RAG | 全量 chunk 加载到内存做余弦相似度 | 知识库增长后会 OOM |

---

## 2. 已完成事项

| # | 事项 | 说明 |
|---|------|------|
| 1 | P0 安全修复（Token 日志 / CORS / 认证绕过）✅ | Login.tsx Token 日志移除，CORS 改为明确域名，认证绕过修复 |
| 2 | ErrorBoundary ✅ | 根路由级别已包裹，防止全局白屏 |
| 3 | 死代码清理 ✅ | src/pages/ 根目录遗留页面已删除 |
| 4 | projects.py 拆分 3,392→455 行 ✅ | 拆出 projects_memory、projects_files、projects_briefing、projects_deps 等子路由 |
| 5 | 子路由拆分（memory/files/briefing/deps）✅ | 大路由已按功能域拆分 |
| 6 | API 超时调整 ✅ | AI 端点超时从 10s 调整为 60-120s |
| 7 | PM2 max_memory_restart ✅ | 内存泄漏时自动重启已配置 |
| 8 | CI 测试门禁 ✅ | pytest + npm test 已加入 CI 流程 |
| 9 | PostgreSQL 安装 + 测试 DB ✅ | 测试环境已独立数据库 |
| 10 | 会前简报→Chat 自动发送 ✅ | 北极星场景核心链路打通 |
| 11 | 项目空间 Phase 2（auto-md）✅ | 上传文件自动转 Markdown |
| 12 | Memory Operations 批量治理 ✅ | 支持批量处理和安全重试策略 |
| 13 | 干系人变更历史 ✅ | 结构化干系人基础维护已落地 |
| 14 | 前端 hook 测试（覆盖率 27%→30%）✅ | hooks/ 目录已纳入测试统计 |
| 15 | console.log / print() 清零 ✅ | 前端 59 处、后端 print() 已全部清除 |
| 16 | .env 生产密钥从 Git 移除 ✅ | 已从 Git 索引中移除 |
| 17 | Markdown 链接净化 ✅ | sanitizeMarkdownHref 集中限制协议白名单 |
| 18 | 干系人结构化 CRUD + AI 上下文消费 ✅ | 独立表、迁移、CRUD API、AI prompt 拼装已落地 |

---

## 3. 待办事项 — 生存层（本周必须做）

这几件事不做，数据泄露或丢失一次，前面所有功能建设都归零。

| # | 任务 | 优先级 | 预计 | 状态 |
|---|------|--------|------|------|
| 1 | Nginx HTTPS + gzip + 安全头（X-Frame-Options、CSP、HSTS） | P0 | 2h | 待做 |
| 2 | 数据库定时备份（pg_dump 每日轮转，保留 7 天） | P0 | 30min | 待做 |
| 3 | 修复 11 个子路由测试适配（projects_memory/files/briefing/deps 等） | P1 | 1-2h | 待做 |
| 4 | SSH Key 认证替代密码认证（deploy.yml:56 — password: ${{ secrets.SERVER_PASSWORD }}） | P1 | 30min | 待做 |
| 5 | Nginx API 代理加 client_max_body_size（nginx_ariaai.conf） | P1 | 15min | 待做 |

**安全剩余项**（09 复审仍有 27 项未修复）：

| # | 问题 | 位置 | 说明 |
|---|------|------|------|
| 1 | Token 存储在 localStorage（XSS 可窃取） | AuthContext.tsx:30 | 当前无 httpOnly cookie 方案 |
| 2 | 无 Token 过期/刷新机制 | AuthContext.tsx | Token 永不过期，泄露后无法自动失效 |
| 3 | RAG 全量加载 chunk 到内存做余弦相似度 | rag.py:114 | O(N) 复杂度，知识库增长后 OOM |
| 4 | 内存限速在多 worker 下失效 | auth.py:17 | PM2 单 worker 下暂可接受，扩 worker 后必现 |
| 5 | 内存 Token 缓存非线程安全 | main.py:159 | 需改用 Redis 或线程安全缓存 |
| 6 | 锁字典无限增长（_project_memory_locks 等） | projects.py:145 | 应改用 WeakValueDictionary |
| 7 | f-string 拼接 SQL | main.py:103-109 | SQL 注入风险 |
| 8 | API Key 遮罩仍通过 logger.info() 记录 | claude.py:144-145 | 遮罩前 8 + 后 4，但仍记录到日志 |

---

## 4. 待办事项 — 产品验证（下周开始）

比任何功能都重要。当前阶段产品在没有真实用户闭环验证的情况下，已经完成了大量工程工作。

**行动方案**：

1. **找 1 个种子顾问**，给他开账号，让他在下周的真实客户会议前使用会前简报。
2. **埋 3 个极简观测点**（预计 2h）：
   - 打开会前简报后，有没有点「带着简报开启对话」？（转化漏斗）
   - 聊天里有没有触发干系人捕获？（行为触发是否自然）
   - 有没有把 Skill 结果保存为文档？（回流闭环是否顺手）
3. **每周跟他聊 15 分钟**，只问三个问题：
   - 这周哪个时刻 AriaAI 帮了你？
   - 哪个时刻你觉得它碍事？
   - 如果只能留一个功能，你留哪个？

**验证后决策**：

- 如果反馈好：把会前简报打磨到「顾问愿意在客户面前打开」的程度。
- 如果反馈一般：停下来，不要进入下一个功能，找到卡点。

---

## 5. 待办事项 — 架构收敛（本月做，要克制）

**应该做的**（按优先级排序）：

| # | 任务 | 预计 | 理由 |
|---|------|------|------|
| 1 | clients.py 拆出 clients_memory.py | 1天 | 1887 行，客户 CRUD / 记忆 / 摘要 / 干系人仍混在一个 router |
| 2 | chat_streaming.py 拆分 | 1天 | 1,475 行，SSE 编排 / 工具执行 / PPT 生成 / 制品提取应分模块 |
| 3 | 引入 TanStack Query | 2-3天 | 替代各页面重复的 useEffect + useState + fetch，有缓存、去重、自动刷新 |
| 4 | 前端覆盖率 30%→40% | 1-2天 | pages/、hooks/ 纳入统计，补交互测试 |
| 5 | 后端 0% 模块骨架测试 | 1-2天 | knowledge、settings、templates、schedules、credential_vault、pdf_translation 等至少每个 3-5 个基础用例 |
| 6 | 输入验证补齐（max_length、日期格式） | 1天 | ProjectCreate、PaymentCreate、MilestoneCreate、LoginRequest 等 Schema 缺校验 |
| 7 | 列表接口加分页 | 1天 | 多个 router 列表无分页 |
| 8 | 提取重复代码（系统提示词、API Key 获取） | 1天 | claude.py vs openai_compat.py ~100 行重复 |
| 9 | 状态值使用 Enum | 0.5天 | "lead"/"opportunity"/"won" 等散落各处 |
| 10 | 统一迁移策略 | 0.5天 | SQLModel.metadata.create_all / Alembic / database.py 轻量补齐三条路径并存 |

**渐进式拆分策略**：不需要一口气拆完，每次改哪个模块就把那部分拆出去。例如改 projects_memory 时顺手把相关端点迁到子路由。

**前端额外治理点**：

- ProjectMemoryTab.tsx、ProjectStakeholdersTab.tsx、ProjectChatMainPanel.tsx 待拆分
- MemoryOperationsSettings 后续可拆成子组件
- ClientStakeholdersStructuredCard.tsx 已承载较多表单与展示逻辑，后续应拆成 form、group summary、card list 三个子组件
- 客户详情页右侧栏包含 Skill、记忆和干系人，后续可考虑拆成子 Tab 或锚点导航

---

## 6. 不建议本月做

| 事项 | 为什么暂缓 |
|------|-----------|
| RAG 引入 pgvector / rerank | 当前知识库规模小，Python 内存计算还能撑，且这不是北极星场景的瓶颈 |
| DeepSeek / 更多 Provider | 当前 Claude + Kimi 已覆盖主力场景，多 Provider 是运维负担 |
| 前端无障碍全面改造 | 内部工具，用户是顾问团队，可排在商业化之后 |
| 记忆版本化的任意版本互比 | 已有快照 + diff + 回滚，够用了 |
| CSS 全面 Tailwind 化 | index.css 1029 行确实大，但不影响用户 |
| types/api.ts 拆分（794 行） | 不影响功能，优先级低 |
| 前端 SSR / SSG | SPA 对内部工具可接受 |
| Playwright E2E | 核心链路尚未稳定，E2E 投入产出比低 |
| Sentry / DataDog 集成 | 先把备份和安全做好，再考虑可观测性 |

---

## 7. 关键指标（建议每周看一次）

| 指标 | 当前 | 目标（1 个月后） |
|------|------|----------------|
| P0 安全漏洞 | 3 个 | 0 个 |
| 后端测试覆盖率 | 55% | 65% |
| 前端真实覆盖率 | ~30% | 40% |
| 前端 ErrorBoundary | 已落地（根路由） | ≥3 个（关键子路由） |
| 数据库备份 | 已建立 | 每日自动备份 + 恢复验证 |
| 种子顾问周活 | 0 | ≥1 |
| 会前简报转化率 | 未知 | 有数据 |
| 0% 覆盖模块数 | 后端 12 个 | ≤5 个 |
| God Files | projects.py 已拆，clients.py/chat_streaming.py 未拆 | 均 <1000 行 |

---

## 8. 详细技术诊断（附录）

### 8.1 前端评估

| 类别 | 评分 | 说明 |
|------|------|------|
| 架构 | 7/10 | 按功能组织、良好的 hooks 抽取、存在死代码和重复 |
| TypeScript | 7/10 | 严格模式开启，但有 unknown/any 泄漏 |
| 安全 | 5/10 | console.log 已清理，但 localStorage 存储 Token 仍在 |
| 性能 | 6/10 | 良好的 memoization，但 warmup 抵消代码分割、Context 未 memoize |
| 无障碍 | 3/10 | 仅约 20 个 aria-label，无焦点管理 |
| 测试 | 3/10 | Vitest 已接入，但 coverage 未统计 pages/（最大代码量），缺少交互测试 |
| 错误处理 | 4/10 | ErrorBoundary 已落地（根路由），但静默 catch 仍在 |
| 代码卫生 | 6/10 | console.log 已清零，但大 CSS、大 types 文件仍在 |

**前端架构问题清单**：

| 问题 | 位置 | 状态 |
|------|------|------|
| 40 个重复的 lazy() 包装器 | App.tsx（403 行） | 未提取 lazyPage 工具函数 |
| warmPrimaryRoutes 抵消代码分割 | routeLoaders.ts:42 | 建议改为 hover 预加载 |
| ToastContext ctx 对象每次渲染重建 | ToastContext.tsx:76 | 待 useMemo |
| AuthContext login 函数未 memoize | AuthContext.tsx:29 | 待 useCallback |
| Layout navItems 数组每次渲染重建 | Layout.tsx:30 | 待 useMemo |
| Google Fonts @import url() 阻塞渲染 | index.css:2 | 改为 preload + font-display: swap |
| 静默 catch(() => {}) | Layout.tsx:41,51 | 用户无反馈 |
| catch(err: any) 绕过类型检查 | Login.tsx:44 等 | 待修复 |

**前端状态管理缺失**：

无服务端状态管理库（TanStack Query）。每个页面手动 useEffect + useState 管理 loading/error/data，导致重复 fetch 模式、无请求缓存、无后台刷新、无乐观更新、无请求去重。

**ESLint 缺失配置**：jsx-a11y、no-console、import 排序、noUnusedLocals/noUnusedParameters。

### 8.2 后端评估

**God Files 状态**：

| 文件 | 原行数 | 当前行数 | 状态 |
|------|--------|----------|------|
| routers/projects.py | 3,354 | ~455 | ✅ 已拆分 |
| routers/clients.py | 1,887 | 1,887 | ❌ 待拆分 |
| services/chat_streaming.py | 1,475 | 1,475 | ❌ 待拆分 |
| types/api.ts（前端） | 794 | 794 | ❌ 待拆分（低优先级） |

**后端性能问题**：

| # | 严重性 | 问题 | 位置 |
|---|--------|------|------|
| 1 | 高 | RAG 在 Python 中对全量 chunk 做余弦相似度（O(N)） | rag.py:114 |
| 2 | 中 | 循环内逐条 session.commit()（N+1 事务） | projects.py:1531, rag.py:191 |
| 3 | 中 | 全表扫描查找客户（Python 内比对） | projects.py:218-223 |
| 4 | 中 | 列表接口无分页 | 多个 router |

**后端并发问题**：

| 问题 | 位置 |
|------|------|
| 锁字典 _project_memory_locks / _project_summary_locks 无限增长 | projects.py:145-146 |
| 后台任务共享全局 engine | projects.py:2037 |
| 内存 Token 缓存非线程安全 | main.py:159 |

**后端代码重复**：

| 重复 | 位置 | 行数 |
|------|------|------|
| 系统提示词构建逻辑 | claude.py:504 vs openai_compat.py:629 | ~100 行 |
| API Key 获取逻辑 | security.py vs openai_compat.py | 多处 |

**Alembic 迁移风险**：代码库同时运行三种迁移路径（SQLModel.metadata.create_all / Alembic / database.py 轻量补齐），三者并存可能导致不一致。

### 8.3 部署评估

**CI/CD（deploy.yml）**：

| 方面 | 状态 | 说明 |
|------|------|------|
| 触发条件 | ✅ | push main/master + workflow_dispatch |
| 并发控制 | ✅ | cancel-in-progress: true |
| 权限最小化 | ✅ | contents: read |
| Node/Python 缓存 | ✅ | npm 和 pip 缓存 |
| 前端测试门禁 | ✅ | npm test (Vitest) 已接入 |
| 后端测试门禁 | ✅ | pytest 已接入 |
| 部署后健康检查 | ⚠️ | deploy.sh 有，CI 中无 |
| 回滚机制 | ❌ | 无 |
| SSH Key 认证 | ❌ | 仍用密码认证（deploy.yml:56） |
| 审批门禁 | ❌ | 无 environment: production |
| 版本追踪 | ❌ | 无 tagged release |

**Nginx 配置**：

| 配置项 | API 代理 | 前端 | 状态 |
|--------|---------|------|------|
| HTTPS/SSL | ✅ 已配置 | ✅ 已配置 | ✅ |
| gzip 压缩 | ✅ 已配置 | ✅ 已配置 | ✅ |
| 安全头 | ✅ 已配置 | ✅ 已配置 | ✅ |
| 静态资源缓存 | ❌ 无 | ❌ 无 | 待做 |
| 速率限制 | ❌ 无 | ❌ 无 | 待做 |
| client_max_body_size | 待配置 | ✅ 100M | 部分 |
| SSE 支持 | ✅ proxy_buffering off | N/A | ✅ |

**PM2 配置（ecosystem.config.js）**：

- ✅ max_memory_restart 已配置
- ❌ error_file / out_file 日志路径
- ❌ merge_logs
- ❌ cluster mode（当前单 worker）
- ❌ exp_backoff_restart_delay

**备份策略**：

- ✅ 已建立定时备份（pg_dump 每日轮转，保留 7 天）
- ❌ 恢复流程未验证
- ❌ 备份无异地存储
- ❌ 迁移前无自动备份

**监控与日志**：

- ❌ 无 APM 集成（Sentry、DataDog 等）
- ❌ 无运行时间监控
- ❌ 无进程崩溃告警
- ❌ 无结构化日志
- ❌ 无日志轮转
- ❌ 无集中日志聚合

### 8.4 测试覆盖详情

**后端（pytest）**：

| 区域 | 状态 | 覆盖率 |
|------|------|--------|
| 认证（登录、限速） | ✅ 有 | — |
| 聊天流式 | ✅ 有 | — |
| 项目 CRUD | ✅ 有 | — |
| 项目记忆 | ✅ 有 | — |
| OpenAI 兼容转换 | ✅ 有 | — |
| 客户记忆任务 | ✅ 有 | — |
| 缓存 / 配置 / 时间工具 | ✅ 有 | — |
| RAG 服务 | ⚠️ 新增测试 | 48% |
| PPT/文档生成（file_generators） | ⚠️ 部分 | 47%（最大单文件缺口，2,232 行） |
| 知识库 CRUD | ❌ 无 | 0% |
| 设置 CRUD | ❌ 无 | 0% |
| 文件上传/下载 | ❌ 无 | 0% |
| 凭证保险库 | ❌ 无 | 0% |
| 调度服务 | ❌ 无 | 0% |

整体后端覆盖率：**55%**（11,487 行，5,211 行未覆盖），12 个模块 0% 覆盖。

**前端（Vitest）**：

| 区域 | 状态 | 覆盖率 |
|------|------|--------|
| Contexts（Auth/Theme/Toast） | ✅ 有 | 95%+ |
| 工具函数（timezone、enums） | ✅ 有 | 90%+ |
| API Client | ✅ 有 | 57% |
| 组件（ThemeToggle、PageTitle、ServiceErrorState） | ✅ 有 | 100% |
| MarkdownRenderer / markdownSecurity | ✅ 有 | 2.7% / 91% |
| pages/ | ❌ 无 | 未统计 |
| hooks/ | ⚠️ 新增 | 27%→30% |
| services/ | ⚠️ 部分 | — |

整体前端覆盖率（按当前统计范围）：**70.5%**，但统计范围故意排除了 pages/ 和 hooks/（最大代码量），真实覆盖率预计 20-30%。16 个测试文件，133 个测试通过。

**覆盖率目标**：

| 阶段 | 后端 | 前端 | 达成标准 |
|------|------|------|----------|
| 当前 | 55% | ~30%（真实） | — |
| 1 周后 | 65% | 40% | 0% 模块清零 + pages 纳入统计 |
| 1 个月后 | 75% | 55% | 核心模块 80%+，E2E 上线 |
| 3 个月后 | 80% | 65% | CI 门禁稳定，新代码覆盖不下降 |

### 8.5 总体评分

| 维度 | 首次评分 | 最新评分 | 变化 | 说明 |
|------|----------|----------|------|------|
| 功能完整度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | — | 功能丰富，AI 能力深度集成 |
| 代码架构 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | — | 模块化合理，hooks 抽取良好，大路由已开始拆分 |
| 安全性 | ⭐⭐ | ⭐⭐⭐ | ↑ | P0 已修复，剩余项已评估 |
| 性能 | ⭐⭐⭐ | ⭐⭐⭐ | — | 无变化，RAG 内存计算仍待优化 |
| 测试覆盖 | ⭐ | ⭐⭐ | ↑ | CI 门禁已建立，后端 55%，前端真实 30% |
| 部署运维 | ⭐⭐ | ⭐⭐⭐ | ↑ | HTTPS + 安全头 + gzip + 备份已落地 |
| 无障碍 | ⭐⭐ | ⭐⭐ | — | 内部工具，暂缓 |
| 代码卫生 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ↑ | console.log/print 清零，死代码清理，projects.py 拆分 |

---

## 9. 项目对话专项技术债（2026-05-15 审阅新增）

### 9.1 已修复 Bug（本次审阅中修复）

| # | Bug | 位置 | 影响 | 修复 |
|---|-----|------|------|------|
| 1 | **@文件提及不注入文件内容** | `context_builder.py` | `@文件` 只是文本标记，AI 看不到文件内容 | ✅ 合并 mention_context.file_ids 到 file_ids |
| 2 | **P3 re-follow-up 截断未追踪** | `chat_streaming.py` | 截断后不显示"继续生成"按钮 | ✅ 添加 p3_double_truncated 标记 |
| 3 | **P3 follow-up 缺少 original_content** | `chat_streaming.py` | Diff 预览无法显示原内容 | ✅ 补全 original_content 字段 |
| 4 | **@提及触发器过于宽泛** | `projectChatMentions.ts` | `email@domain.com` 误触发 | ✅ 增加前置字符检查 |
| 5 | **mention regex 过于宽泛** | `projectChatMentions.ts` | 匹配 `@i:` 等无效格式 | ✅ 改为 `@[fsm]:` |

### 9.2 待修复问题

| # | 问题 | 位置 | 优先级 | 建议 |
|---|------|------|--------|------|
| 6 | sendMessageAsync 无超时/取消 | `useProjectChatComposer.ts` | P1 | 添加 AbortController + 30s 超时 |
| 7 | 自动干系人检测无取消机制 | `ProjectChatTab.tsx` | P1 | useEffect cleanup 或 AbortController |
| 8 | 计划模式双 LLM 调用 | `chat_plan.py` | P2 | 文档化或优化为单次调用 |
| 9 | chat_streaming.py 单文件 1,740 行 | `chat_streaming.py` | P1 | 拆分为 P1/P2/P3/P4 子模块 |
| 10 | 后台任务内存注册表 | `chat_async.py` | P1 | 引入 Redis 或 TaskRun 状态机 |
| 11 | 工具调用代码重复 | `chat_streaming.py` | P2 | 提取 execute_tool_with_artifact() 通用函数 |
| 12 | 硬编码中文提示 | `chat_streaming.py` | P3 | 使用常量或 i18n 字典 |
| 13 | print() 调试语句残留 | `chat_streaming.py` | P3 | 替换为结构化 logging |

### 9.3 安全风险

| # | 风险 | 位置 | 严重程度 |
|---|------|------|----------|
| 1 | Token 存储在 localStorage（XSS 可窃取） | `AuthContext.tsx` | 中 |
| 2 | 工具输入未经 schema 校验直接传递 | `registry.execute()` | 中 |
| 3 | SSE 流无背压控制 | `stream_chat_events()` | 低 |

### 9.4 性能风险

| # | 风险 | 位置 | 说明 |
|---|------|------|------|
| 1 | 上下文构建同步阻塞 | `prepare_chat_runtime()` | 大项目文件多时会慢 |
| 2 | 文件内容全量加载 | `build_project_context()` | 未做分段/摘要 |
| 3 | RAG 全量 chunk 余弦相似度 | `retrieve_structured()` | 知识库增长后 OOM |

---

> **一句话总结**：AriaAI 已经从「能不能做出来」进入「值不值得继续投入」的阶段。技术架构在正确轨道上，但接下来 30 天最重要的事不是写代码，而是让一个真实顾问在真实会议前打开会前简报，然后告诉我们他看到了什么。代码可以慢慢还，方向错了再快也是偏离。
