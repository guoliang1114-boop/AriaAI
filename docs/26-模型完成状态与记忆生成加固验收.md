# 模型完成状态与记忆生成加固验收

日期：2026-09-20。前置运行版本：`1bb53c1`；前置文档版本：`52c5485`。

## 问题与修复

- Kimi、DeepSeek、MiMo 的非流式响应原先在最终正文为空时回退到 `reasoning_content`，可能让内部推理进入摘要或记忆。现在只接受最终正文或完整工具调用；BigModel 一并使用相同校验。
- 所有非流式路径原先缺少明确的截断检查。现在供应商声明达到输出上限时立即失败，即使正文表面上是完整 JSON。Claude HTTP 与 SDK 同时接入，并统一多文本块、工具调用返回方式。
- 损坏的工具参数原先可能静默变成 `{}`。现在拒绝非对象参数、重复 JSON 键、非有限数值、无效调用身份与重复调用 ID；任一调用无效则整份响应失败。
- 客户记忆提示未明确 `client_profile` 的字符串类型。项目四个标量槽位同样补齐类型约束；项目到客户的记忆晋升复用完整客户类型规则。
- 完整/局部重建、完整回退及两种晋升路径使用固定 system 指令。资料、名称和已有记忆以转义 JSON 进入来源区块，不能通过原始结束标签跳出区块。

## 验证

- 最终 Provider 完成状态与 Claude SDK 专项：**73 passed**。
- 记忆来源编码、字段规则、OpenAI 兼容转换、Claude 提示等专项曾合计 **115 passed**；后续新增的三条畸形状态测试包含在上述最终 73 项中，不能将这两组数字相加。
- 独立本机 PostgreSQL 16.13 受影响回归首轮：**651 passed、3 failed、1 skipped**。三个失败均为旧测试桩未接收新增的 `system` 参数，已更新并增加固定指令断言；三个用例及两条客户记忆流程复验 **5 passed**。既有跳过项为已移除的售前模板 bootstrap。测试数据库仅通过临时 Unix socket 提供服务，与应用/生产数据库隔离。
- 客户和项目的持久化回归经实际 Provider 适配器确认：仅有推理、明确截断、损坏工具参数均不会替换已有业务记忆、增加版本或创建快照，也不会触发额外模型调用。
- 权限、撤权、来源变化、取消代次、记忆候选、HITAS、流式观察及对话回归均在受影响集合中。两份发布工作流已加入新用例；YAML 与 Python 语法检查、`git diff --check` 通过。
- 首次扩大回归未配置独立数据库，默认本机 PostgreSQL 连接失败；之后建立独立 PostgreSQL 完成上述验证。保留既有 Python 3.9 / LibreSSL 的 urllib3 警告。

## 边界

此改动不新增数据库迁移，不改变 Product Run Event 或流式协议。来源编码与提示是注入防御的一层，不能单独保证语义注入免疫；原生来源 ACL、严格业务解析、最终授权与 HITAS 仍是执行边界。缺省完成原因的旧代理仍允许返回合法最终内容，未报告的截断继续依赖下游结构校验。

本次未自动重建客户/项目记忆，也未启用新的知识索引配置。历史生产失败记录不能据代码修复直接宣告恢复；实际重建需要走原生业务流程。本轮不关闭知识库、Run Harness 与安全三条长期事项。

## 发布

通过 main 推送触发 GitHub Actions；部署结果和上线后健康检查待本轮执行结束后补录。
