/**
 * New-project entry point — re-exported from the codex redesign at
 * codex/CxNewProject. The legacy 3-panel implementation (AI assistant
 * + basics form + stage selector) is retired with the codex rewrite.
 */
export { CxNewProject as NewProject } from './codex/CxNewProject'
